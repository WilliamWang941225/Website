{
  "name": "website-tsx",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-resources": "node scripts/generateResources.mjs",
    "dev": "npm run generate-resources && vite",
    "build": "npm run generate-resources && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  }
}
